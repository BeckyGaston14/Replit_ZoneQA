Add this workflow and terminology correction after the queued form cleanups:

1. Bassett Test Runs currently support workflow statuses in the backend and list filter but the unified create/edit form does not expose the workflow status. Add a clearly labeled “Workflow status” dropdown to Bassett-only Test Run create/edit with: New, Triaged, In Progress, Blocked, Resolved, Closed. New records default to New.
2. Rename the existing “Status / verdict” field to “Test result” everywhere it appears. Do not change its stored result values or calculation behavior.
3. Add a prominent “Mark as Triaged” action on an active New Test Run’s detail view for authorized writers. It must update status to Triaged, record triaged_by user ID/name and triaged_at timestamp, append history/audit activity, use concurrency protection, show clear success/failure feedback, and immediately refresh the record, list, filters, and metric cards. Do not show it for already-triaged or archived records.
4. Ensure ordinary editing can change workflow status and that status history records old and new values. Preserve linked findings, attachments, scores, and all other run data.
5. Rename the Bassett Test Runs metric card “New” to “Untriaged Test Runs,” with helper text “Workflow status is New.” The metric remains the count of active, non-archived Bassett-only test runs whose workflow status is exactly New.
6. Apply the same terminology clarity throughout every page, report, dashboard card, filter, table, detail view, export, and PDF: use “Workflow status” for New/Triaged/In Progress/Blocked/Resolved/Closed; “Test result” for Pass/Pass with Notes/Partial/Fail/Blocked/Not Evaluated; “Severity” for impact; and “Scenario coverage” for evaluated Test Bank coverage. Do not blindly rename database/API fields or alter calculations. Comparison-specific labels should remain comparison-specific when accurate.
7. Audit metric labels across all pages for ambiguity. Prefer descriptive names such as “Untriaged Test Runs” over “New,” and include concise scope text. Do not change metric populations, double-count records, or merge Bassett-only and Model Comparison populations.
8. Add backend and frontend tests for status updates, triage metadata/history, permissions, archived restrictions, concurrency, label consistency, and metric refresh. Run full suites/build. Commit, but do not publish or alter existing production records.