Implement these six approved ZoneQA improvements against the current Replit workspace. Preserve all production records and existing behavior. Work in small commits and do not publish.

1. Attachment download reliability: diagnose the Bassett Test Run attachment button end-to-end. It currently shows stored PDFs but a click produces neither a browser download nor a new tab. Fix the authenticated blob download so it reliably downloads using the original filename; show a clear error toast on 404/403/storage failure. Add frontend and backend regression tests.
2. Repair Test Bank scenario A-17 and audit all active Test Bank scenarios for the same hidden completeness failure. The UI shows A-17 as complete, but PostgreSQL rejected it as incomplete. Fix the actual stored/seed/migration normalization without renaming stable IDs or deleting tests. Add an integrity check that identifies incomplete active scenario definitions and a safe repair for seed-compatible missing fields.
3. Dashboard small-sample warning: when an aggregate rate/score is based on fewer than 5 eligible evaluated records, show a consistent “Limited data — N evaluated records” warning on the dashboard metric and executive summary. Do not change calculations.
4. Data Integrity loading: stop automatically rerunning the expensive validation on every visit. Show the cached/latest result immediately with its timestamp and add an explicit “Run integrity checks” button. During a manual run show progress and disable duplicate runs. Preserve permissions and repair confirmations.
5. Standardize Testing Projects controls: change “Export” to “Export CSV” and “New” to “New Testing Project”; confirm icons and button order match other list pages. Add or update UI tests.
6. Turn-level scenario evaluations for uploaded multi-turn conversations: keep one parent Bassett Test Run and one authoritative attachment, but allow each conversation turn to be linked to a Test Bank scenario and evaluated independently. Add stable turn IDs, per-turn scenario selector, optional per-turn result and notes, with no duplicated attachment. The parent record retains a primary scenario. Coverage should count a scenario only when its latest eligible parent or turn evaluation has a qualifying evaluated result; never double-count the same scenario. Findings can link to a specific turn. Existing records must remain compatible.

Run focused backend/frontend tests, production build, migration/seed validation, and report commits and remaining risks. Do not publish.