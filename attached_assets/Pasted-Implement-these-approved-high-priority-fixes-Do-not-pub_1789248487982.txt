Implement these approved high-priority fixes. Do not publish until all verification passes.

1) Release Readiness minimum evidence threshold: require at least 50 qualifying tests in the selected release-readiness scope before the system can recommend Go or No-Go from pass-rate logic. When qualifying_count < 50, show a prominent neutral state “Insufficient Evidence” with “X of 50 qualifying tests completed” and do not display Go. Critical blockers may still be surfaced, but must not convert insufficient evidence into Go. Apply consistently to UI, API/report/export/PDF calculations and both scope selectors. Centralize the threshold as one constant/configuration value rather than duplicating 50. Add boundary tests for 0, 1, 49, 50 and >50, including critical blockers. Preserve current metric definitions and existing data.

2) Property duplicate prevention: investigate every property creation/selection path, especially unified Bassett-only and Model Comparison forms. Selecting an existing property must only store/reference its existing ID and must never create another property record. New-property creation must normalize address data (trim/collapse whitespace; case-insensitive; normalize common punctuation and state/ZIP comparison) and reject or guide the user to an existing likely duplicate. Enforce this server-side transactionally as well as in the UI so double clicks/retries/concurrent requests cannot duplicate a property. Do not silently merge distinct properties. Add regression tests proving repeated selection of the same existing property and repeated identical create requests result in one property and stable linked relationships.

3) Existing Milwaukee duplicate: first inspect the two exact 6442 N 76th St, Milwaukee, WI 53223 property records and all inbound references, attachments, audit/history links and timestamps. Safely consolidate them into one canonical property, re-point every reference, preserve attachments/history, and remove/archive only the redundant record after verifying zero remaining inbound references. This production data cleanup is explicitly authorized. Record an auditable note/log of the merge. Do not modify unrelated production records. If the records contain conflicting material fields, stop and report rather than guessing.

4) Data Integrity: add semantic duplicate-property detection using normalized municipality/address/city/state/ZIP, with clear candidate reporting. It must not report Clean when exact duplicate candidates exist.

Run the full relevant backend/frontend tests, production build, and development runtime checks. Independently review for blockers. Commit the code. Report the exact data merge outcome separately, including canonical/redundant IDs and reference counts. Do not publish; I will publish after reviewing the result.